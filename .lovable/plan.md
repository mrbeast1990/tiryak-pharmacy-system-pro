

# خطة تغيير دور المستخدم إلى مدير

## ملخص
تغيير دور المستخدم "عبدالوهاب" (whab.alzwawy10@hotmail.com) من `member` إلى `admin`.

## التغييرات المطلوبة

### تحديث قاعدة البيانات
تحديث جدول `profiles` لتغيير الدور:

```sql
UPDATE profiles 
SET role = 'admin' 
WHERE id = '50022a12-ca7a-4cb7-8187-acc0d6da8996';
```

## الصلاحيات التي سيحصل عليها بعد التغيير

| الصلاحية | الوصف |
|----------|-------|
| view_all | عرض جميع البيانات |
| edit_all | تعديل جميع البيانات |
| delete_all | حذف جميع البيانات |
| export_revenue_pdf | تصدير تقارير الإيرادات |
| export_shortages_pdf | تصدير تقارير النواقص |
| manage_users | إدارة المستخدمين |
| register_revenue_all | تسجيل إيرادات جميع الفترات |
| manage_shortages | إدارة النواقص |
| view_reports | عرض التقارير |

## ملاحظة مهمة عن كلمة المرور
كلمات المرور مشفرة ولا يمكن الوصول إليها. الخيارات المتاحة:
1. استخدام كلمة المرور التي تم إنشاء الحساب بها
2. إعادة تعيين كلمة المرور عبر البريد الإلكتروني
3. إعادة التعيين من لوحة Supabase

